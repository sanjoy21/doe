<?php
// $loginpage = 1;
// include "mysql.php";

// if (isset($session_id) && $session_id) {
//     $row = db_query_first( "select userid, redirectURL from user where id = $session_id " );
//     $redirectURL = ($row["redirectURL"]) ? $row["redirectURL"] : "/login.php";
//     header( "Location: $redirectURL" );
//     exit; // Important to exit after header
// }
// else
// {
//  $redirectURL = ($row["redirectURL"]) ? $row["redirectURL"] : "/login.php";
//     header( "Location: $redirectURL" );
//  exit; // Important to exit after header
// }
echo "<h1 align='center'>Welcome to DOE</h1>";
?>